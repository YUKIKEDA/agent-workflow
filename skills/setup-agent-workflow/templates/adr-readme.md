# ADR（ループ改善）

このフォルダは **開発ループの改善** だけを置く。プロダクトの決定済み事項の正本は GitHub の親 Issue（ラベル `loop`）。

- ファイル: `NNNN-short-slug.md`（4 桁連番）
- 状態: `proposed` → 人間承認後 `/accept-adr` で `accepted`
- ひな形:

```markdown
# 題

- 状態: proposed
- 日付: YYYY-MM-DD
- 元 Issue: #N

## 文脈

## 決定

## 結果
```
